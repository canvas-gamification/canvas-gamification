def get_submission_analytics(submission):
    # 1 check if the submission analytics exists
    # 1.1 If yes return that
    # 2 calculate the submission analytics
    # 3 save it in the database (make sure to replace the old one)

    return {
        'time_spent': 1,
    }


def get_question_analytics(question, cache: bool):
    # 1 if cache and question analytics exists return that
    # 2 calculate question analytics and save it in the database
    # 3 make sure to replace the old one

    # if the question analytics exists and cache is true but the cache is old
    # still calculate it

    # move this to a separate file

    pass
