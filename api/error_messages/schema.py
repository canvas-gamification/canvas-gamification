class SCHEMA:
    REQUIRED = "Schema name is required."
    INVALID = "Schema name is invalid."

    ERROR_MESSAGES = {
        'required': REQUIRED,
        'null': REQUIRED,
        'blank': REQUIRED,
        'invalid': INVALID,
    }
